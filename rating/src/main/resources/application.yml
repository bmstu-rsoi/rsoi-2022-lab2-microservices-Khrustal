spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:postgresql://localhost:5432/
    password: postgres
    username: postgres
    driver-class-name: org.postgresql.Driver
server:
  port: 8050
